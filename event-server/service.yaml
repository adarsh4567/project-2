apiVersion: v1
kind: Service
metadata:
  name: event-server
  namespace: event-server
  labels:
    app: event-server
spec:
  type: ClusterIP
  selector:
    app: event-server
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
